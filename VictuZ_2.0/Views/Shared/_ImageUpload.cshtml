@model VictuZ_2._0.Models.Newss.News

<div class="form-group mb-3">
    <label for="imageFile" class="control-label">Image (optional)</label>
    <div class="image-dropzone border p-3 text-center"
         style="position: relative; height: 200px; overflow: hidden; cursor: default;"
         data-has-image="@(!string.IsNullOrEmpty(Model.ImageUrl)).ToString().ToLower()"
         data-image-url="@Model.ImageUrl">
        <input type="file" name="imageFile" id="imageFile" accept="image/*" class="form-control" />
        <div class="image-preview mt-2">
            @if (!string.IsNullOrEmpty(Model.ImageUrl))
            {
                <img src="@Model.ImageUrl" alt="Current Image" class="img-fluid mb-2 preview-image" />
                <p>Drag & Drop a new image here or use the file input above</p>
            }
            else
            {
                <p>Drag & Drop an image here or use the file input above!</p>
            }
        </div>
    </div>
    <span asp-validation-for="ImageUrl" class="text-danger"></span>
</div>
