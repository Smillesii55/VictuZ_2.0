@model IEnumerable<VictuZ_2._0.Models.Sessions.Session>

@{
    ViewData["Title"] = "Agenda";
}

<h1>Agenda</h1>

<!-- Alleen BoardMembers kunnen nieuwe sessies aanmaken en verwijderen -->
@* Controleer of de gebruiker is ingelogd en de rol "BoardMember" heeft *@
@if (User.Identity.IsAuthenticated && User.IsInRole("BoardMember"))
{
   <p>
            <a asp-action="Create" class="btn btn-primary">Create New</a>
            <a asp-action="DeleteSelection" class="btn btn-danger">Delete a Session</a>
   </p>
}

<!--Show cards for each task model, grouped per month-->
@foreach (var sessionsInMonth in Model.GroupBy(x => x.ActivityDate.Month))
{
    <!--Write the month name with the first letter capitalized-->
    <h2 class="title-text">@sessionsInMonth.First().ActivityDate.ToString("MMMM")</h2>

    <div class="row">
        @foreach (var session in sessionsInMonth)
        {
            <div class="col-md-4">
                <div class="card mb-4 shadow-sm">
                    <div class="card-body">
                        <h5 class="card-title">@session.Title</h5>
                        <p class="card-text">@session.Description</p>

                        <a asp-action="Details" asp-route-id="@session.Id" class="btn btn-primary">Details</a>

                        @if (User.Identity.IsAuthenticated && User.IsInRole("BoardMember"))
                        {
                            <a asp-action="Edit" asp-route-id="@session.Id" class="btn btn-warning">Edit</a>
                            <a asp-action="Delete" asp-route-id="@session.Id" class="btn btn-danger">Delete</a>
                        }
                    </div>
                </div>
            </div>
        }
    </div>
}

<!--
<tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @if (User.Identity.IsAuthenticated && User.IsInRole("BoardMember"))
                    {
                        <p>
                            <a asp-action="Edit" asp-route-id="@item.Id">Edit</a>
                        </p>
                    }
                    <a asp-action="Details" asp-route-id="@item.Id">Details</a>
                    @if (User.Identity.IsAuthenticated && User.IsInRole("BoardMember"))
                    {
                        <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                    }
                </td>
            </tr>
        }
    </tbody>


<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ActivityDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EndDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreatedBy.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Location.Name)
            </th>
            <th></th>
        </tr>
    </thead>
 </table>
    -->


